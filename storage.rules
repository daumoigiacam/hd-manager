rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Storage is not used by the current application. Keep this bucket
    // private until tenant-scoped paths and emulator coverage are implemented.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
